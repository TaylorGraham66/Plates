specVersion: "v1beta"
serviceId: "Plates"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Plates-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
